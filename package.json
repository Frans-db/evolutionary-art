{
  "name": "evolutionary-webdesign",
  "version": "1.0.0",
  "description": "Approximate input images using triangles with evolutionary algorithms.",
  "main": "index.js",
  "scripts": {
    "build": "tailwindcss -i tailwind.css -o index.css",
    "watch": "tailwindcss -i tailwind.css -o index.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
